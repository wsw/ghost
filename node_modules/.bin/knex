#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../knex/bin/knex" "$@"
  ret=$?
else 
  node  "$basedir/../knex/bin/knex" "$@"
  ret=$?
fi
exit $ret
